import type { ExerciseFormat } from './learning-engine.ts';
export interface QuizQuestion{id:string;before:string;answer:string;after:string;translation:string;options:string[]}
export const quizQuestions:QuizQuestion[]=[{id:'phrase-3',before:'in front of the ',answer:'elephants',after:'.',translation:'ゾウたちの目の前です。',options:['giraffes','entrance','elephants','cameras']},{id:'phrase-4',before:'They have really, really, really ',answer:'long trunks',after:'.',translation:'本当に、本当に、とても長い鼻があります。',options:['short tails','long trunks','large wings','tiny ears']},{id:'phrase-5',before:"And that's ",answer:'pretty much',after:' all.',translation:'そして、だいたいそれくらいです。',options:['right away','at first','by chance','pretty much']}];
export interface QuizEntry{phraseId:string;format:ExerciseFormat;retryIndex:0|1|2;filler?:boolean}
export function normalizeAnswer(s:string):string{return s.normalize('NFKC').toLowerCase().replace(/[’‘]/g,"'").replace(/\bi'm\b/g,'i am').replace(/\bthat's\b/g,'that is').replace(/\bdon't\b/g,'do not').replace(/[^\p{L}\p{N}\s']/gu,' ').trim().replace(/\s+/g,' ')}
export const correctAnswer=(target:string,value:string)=>normalizeAnswer(target)===normalizeAnswer(value);
export function queueRetry(queue:QuizEntry[],index:number,random=Math.random):QuizEntry[]{const current=queue[index]!;if(current.retryIndex===2||current.filler)return queue;const offset=2+Math.min(2,Math.max(0,Math.floor(random()*3)));const updated=[...queue];const alternatives=quizQuestions.filter(q=>q.id!==current.phraseId);while(updated.length-index-1<offset){const q=alternatives[(updated.length-index-1)%alternatives.length]!;updated.push({phraseId:q.id,format:current.format,retryIndex:0,filler:true})}updated.splice(index+offset+1,0,{...current,retryIndex:(current.retryIndex+1) as 1|2});return updated}
