---
document: youtube-dynamic-ingestion-design
version: 0.3
status: user-confirmed-ingestion-and-free-limit-rules
created: 2026-09-06
updated: 2026-09-06
owner: LilQ
related:
  - 17_パイロットMVP要件定義_v0.2.md
  - 19_LearnerModel_データ計測仕様_v1.0.md
---

# Lingoflow YouTube動的追加・字幕処理設計 v0.3

## 1. 目的

固定教材だけでなく、新しく公開され続けるYouTube動画をLingoflowへ追加できるようにする。同時に、再生できること、字幕を取得・加工できること、字幕や場面を保存して学習に使えることを別々に判定する。

## 2. 入口

- アプリ内でYouTube URLを貼り付ける
- YouTubeアプリやブラウザの共有シートからLingoflowへ送る
- 運営管理画面から複数URLを登録する
- 将来は、登録チャンネルの新着候補を取得する。ただし自動教材化はしない
- アプリ内でテーマまたは検索語からYouTube動画候補を探す
- Lingoflowでフォローしたチャンネルの動画から選ぶ

検索はYouTube Data API等の公式に許可された経路を基本とする。検索結果の表示と、字幕を使ったLingoflow教材化は別の処理として扱う。

## 3. 全体処理

```text
URL受領
  → YouTube動画IDへ正規化・重複確認
  → 動画メタデータと埋め込み可否を確認
  → 字幕供給経路と利用条件を確認
  → 言語・字幕品質・時間情報を検査
  → 表現候補、訳、解説、練習案を生成
  → 自動品質検査
  → 必要に応じて人が確認
  → 個人用または公開教材として配信
  → ユーザーが開いた時または再生エラー時に公開状態・字幕版を再確認
```

## 4. 字幕供給の優先順位

1. 権利者・チャンネル運営者から提供された時刻付き字幕
2. ユーザーが利用権限を確認したうえで提供する字幕
3. 正式な契約・許諾のある字幕供給サービス
4. 字幕なし動画へのASR。音声処理・保存が許される場合だけ実行

一般のYouTube動画について、公式APIだけで任意動画の字幕本文を取得できることは前提にしない。字幕本文を確保できない場合は「視聴のみ」または「字幕を提供」とする。

## 5. 機能ゲート

| 判定 | 再生 | 同期字幕 | 表現抽出 | 場面復習 |
|---|---:|---:|---:|---:|
| 埋め込み可・字幕利用可・加工可 | ○ | ○ | ○ | 条件内で○ |
| 埋め込み可・字幕本文なし | ○ | × | × | × |
| 埋め込み可・字幕は表示のみ | ○ | 条件内 | × | × |
| ASR処理の許諾あり | ○ | 生成後○ | ○ | 条件内で○ |
| 非公開・削除・埋め込み不可 | × | × | × | × |

## 6. 処理ジョブ

### A. URL検証

- `youtube.com/watch`、`youtu.be`、`youtube.com/shorts`などから動画IDを抽出する
- 再生リストURLは最初の動画だけを暗黙登録せず、一覧として確認させる
- 同じ動画IDの既存レコードがあれば再利用可能な結果を探す

### B. メタデータ確認

- タイトル、チャンネル、長さ、サムネイル、公開状態、埋め込み可否を記録する
- Shorts、ライブ、長時間動画は対応状態を分ける
- 年齢・地域制限などで再生できない場合をエラー理由へ変換する
- YouTube公式プレイヤーで必要なブランド表示とリンクを維持する
- 30分を超える動画は、ユーザーに30分以内の連続した学習範囲を選ばせる

### C. 字幕確認

- 元言語、手動／自動、時刻粒度、字幕版を記録する
- 長い欠落、重複、極端に短い区間、文字化け、言語不一致を検査する
- 単語時刻がない場合、フレーズ巻き戻しは字幕セグメント単位で提供する
- 利用可能な字幕経路を優先し、利用できない場合だけ処理条件を確認してASRを試す

### D. 学習データ生成

- 1動画から候補表現を抽出するが、画面へ出すのは1〜3個に絞る
- 候補は初学者への有用性、場面依存性、頻度、長さ、発話への使いやすさで順位づける
- 翻訳、短い意味、使用場面、穴埋め、発話テンプレートを生成する
- AI生成物にはモデル、プロンプト版、生成日を付ける
- 字幕の語彙、表現、文法、文長、話速を解析し、ユーザー別の推定理解度計算に使う
- 対象言語が主に話され、音声が明瞭で、学習可能な文章が十分あるかを判定する

### E. 品質確認

- 字幕と音声のずれ
- 訳の意味反転・主語誤り
- 不適切な教材候補
- フレーズ開始・終了時刻
- 巻き戻し時に語頭が欠けないか
- 動画の権利・公開範囲

## 7. フレーズ巻き戻し

各表現は `phrase_start_sec`、`phrase_end_sec`、`replay_preroll_ms` を持つ。

```text
seek先 = max(0, phrase_start_sec - replay_preroll_ms / 1000)
停止先 = phrase_end_sec + 0.2秒
```

- 標準prerollは700msとし、語頭が欠ける教材は人または自動検査で調整する
- 「1回」は停止先で一時停止する
- 「リピート」は停止先からseek先へ戻し、ユーザーが止めるまで続ける
- ユーザーが通常シークした場合はリピートを解除する
- バッファリングが続く場合は最大回数で止め、通常再生へ戻れるようにする
- フレーズ時刻がない場合は字幕の `start_sec` と `end_sec` を使う

## 8. キャッシュと更新

- 動画ID、字幕言語、字幕版、解析版をキャッシュキーにする
- 他ユーザーとの解析共有は権利条件が許す場合だけ行う
- YouTube動画本体をLingoflowのキャッシュへ保存しない
- 動画の公開状態・埋め込み可否を全件定期巡回しない
- ユーザーが動画詳細または学習画面を開いた時にオンデマンド確認する
- YouTubeプレイヤーが非公開、削除、埋め込み不可などのエラーを返した時に再確認する
- 字幕が変更されたら既存表現の時刻を再検証し、古い版を即時上書きせず差し替え履歴を残す

再確認要求は動画ID単位で短時間キャッシュし、同じ動画を複数ユーザーが続けて開いても外部APIを重複呼び出ししない。APIクォータ消費を観測し、上限接近時は古いキャッシュ表示と再試行案内へ切り替える。

## 9. 失敗時の扱い

| 理由 | ユーザー表示 | 次の操作 |
|---|---|---|
| URL不正 | URLを確認できません | 貼り直す |
| 動画なし／非公開 | この動画は現在再生できません | 別動画を探す |
| 埋め込み不可 | Lingoflow内では再生できません | YouTubeで開く |
| 字幕なし | 学習用字幕を準備できません | 字幕提供／視聴のみ |
| 処理失敗 | 準備を完了できませんでした | 枠返還／再試行／報告 |
| 音声不明瞭・文章不足 | この動画では精度の高い教材を作れません | 枠返還／別動画 |
| 字幕品質不足 | この動画では字幕品質が足りません | 枠返還／別動画 |

## 9.1 解析枠・チケット

有料化後の解析枠は、開始時に予約し、教材生成の成功時に確定消費する。予約、確定消費、返還は追跡可能な台帳イベントとして記録する。

| 終了理由 | 枠の扱い |
|---|---|
| 教材生成成功 | 1枠を確定消費 |
| ユーザーが処理開始前にキャンセル | 全量返還 |
| 通常処理開始後にユーザー都合でキャンセル | 原則として消費。料金設計確定時に表示文言を再確認 |
| 音声不明瞭、文章不足、字幕品質不足、言語不一致 | 全量返還 |
| サーバー・AI・外部連携障害 | 全量返還 |
| 解析開始から3分以上完了せず、ユーザーがキャンセル | 全量返還 |
| 同じ動画の共通キャッシュを利用 | プラン設計に従うが、重複したAI実費としては扱わない |

3分は初期値として設定可能にし、実測した通常処理時間に応じて管理設定で変更できるようにする。返還処理は冪等にし、再試行や二重通知で複数回返還しない。

### テスト期間の無料解析上限

- 新規URLから行うAI解析だけを、1日3動画・月20動画までに制限する
- 既に解析済みのライブラリ動画の視聴・学習・復習は無制限にする
- 解析済み動画を別のユーザーが学ぶ場合も、そのユーザーの新規解析数へ数えない
- 日次・月次の残数は「AI教材を作る」画面と解析中一覧で表示する
- ホームや通常の学習画面で「月20本まで」とだけ表示しない

ユーザー向けの基本表現は「ライブラリの教材は無制限。あなたが追加する新規動画のAI解析は1日3本・月20本まで」とする。

## 9.2 解析進捗

画面には「動画確認→字幕準備→表現抽出→問題作成→完了」の5段階を表示する。処理が遅い場合も、完了保証のない時刻を断定せず、現在工程とキャンセル可否を示す。

## 10. MVP実装順

1. URL貼り付け、動画ID正規化、公式埋め込み再生
2. テーマ・検索語によるYouTube検索と結果表示
3. 管理側で用意した字幕の取り込みと同期
4. 字幕解析済み動画の推定理解度
5. 表現単位の時刻と巻き戻し
6. バックグラウンド解析状態と通知
7. ユーザー字幕提供フロー
8. 許諾済み動画へのASR
9. チャンネル新着候補の取得

Shortsは同じ解析パイプラインへ通し、縦型プレイヤーの表示だけを分ける。

この順序なら、動画が増える設計を最初から保ちながら、字幕取得が未確定な経路へプロダクト全体を依存させずに検証できる。

## 11. 技術確認で測ること

- URL追加から再生可能判定までの時間
- 字幕準備完了までの時間と失敗率
- 5種類以上のYouTube URL形式を正規化できるか
- フレーズ巻き戻し100回中、語頭・語尾が欠ける割合
- 動画非公開化・字幕変更後に古い教材を停止できるか
- 同一動画の重複解析を避けられるか
- 1動画あたりのAI処理費用と人手確認時間
- システム・素材都合の失敗で解析枠が一度だけ全量返還されるか
- 3分超過キャンセルで解析枠が返還されるか
- 定期全件巡回なしで外部APIクォータを管理できるか

## 12. 未確定事項

- 字幕本文の正式な供給契約または提供者フロー
- ASR対象動画の許諾確認方法
- YouTube APIクォータの警戒値とオンデマンド確認キャッシュ時間
- Shorts、ライブ、Podcastの対応優先順位
- 個人用解析結果を他ユーザーへ共有できる条件
- 人手レビューが必要になる品質閾値
