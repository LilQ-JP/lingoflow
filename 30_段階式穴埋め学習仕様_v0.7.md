---
document: progressive-cloze-learning-spec
version: 0.7
status: user-confirmed-daily-boundary-and-grading-rules
created: 2026-09-06
updated: 2026-09-06
owner: LilQ
related:
  - 19_LearnerModel_データ計測仕様_v1.0.md
  - 26_動画プレイヤー・字幕・AI学習仕様_v0.3.md
---

# Lingoflow 段階式穴埋め学習仕様 v0.7

## 1. 目的

YouTube動画で実際に使われた一文を、その動画・音声・文脈と結びつけたまま穴埋め問題に変え、補助を少しずつ減らして自力想起へ導く。

## 2. 基本画面

- 上部：対象フレーズ、セッション進捗、閉じる
- 中央上：YouTubeプレイヤーまたは場面サムネイル
- 中央：学習言語の一文。対象表現だけを空欄にする
- 下部：学習サポート言語の自然な訳
- 操作：フレーズ再生、もう一度、回答、答えを見る、保存
- 回答後：正答、許容解、文脈上の意味、短い解説、次へ

参考画像の大きなカードと暗い背景は視認性の参考にし、ブランド、アイコン、配置、文章はLingoflow用に設計する。

## 3. 一表現の学習順序

1. 動画内の該当フレーズを字幕つきで再生
2. 対象表現を強調し、自然な訳を表示
3. 対象部分を空欄にして、音声を聞きながら回答
4. 正答後に同じ場面をもう一度再生
5. 必要に応じて発話または文字入力で一度使用
6. 後日の復習で補助を減らした穴埋めを出す

## 4. 段階的なヒント

| 段階 | 表示 | 主な対象 |
|---|---|---|
| 1 理解 | 全文、自然な訳、対象表現の強調 | 初見 |
| 2 支援つき穴埋め | 空欄、訳、必要なら先頭文字 | 初回練習 |
| 3 音声つき穴埋め | 空欄、音声、訳は折りたたむ | 練習中 |
| 4 ヒントなし穴埋め | 文脈文と空欄のみ。音声は再生可能 | 習得確認 |
| 5 応用 | 別の短い文脈で同じ表現を使用 | 定着確認 |

不正解時は一段階下の形式へ戻す。同一形式で異なる日に2回、追加ヒントなし・初回正解した場合だけ次の形式へ進む。ユーザーはいつでも「答えを見る」を選べるが、降格も進級も発生しない。

## 5. 空欄の作り方

- 原則として、単語単体ではなく意味のまとまりで隠す
- 前置詞、助詞、活用を含めて覚える必要がある場合は表現全体を隠す
- 一度に複数箇所を隠しすぎない
- 初級者には短い表現を優先する
- 固有名詞、数字、字幕誤認識箇所は原則として問題にしない
- AIが候補を作り、信頼度が低い問題は自動出題しない

## 6. 回答方法

### 初級者

同じ表現に対して、次の順で自力想起の負荷を上げる。

1. 選択肢：正しい表現を候補から選ぶ
2. 単語並べ替え：分割された語を正しい順序へ並べる
3. 文字入力：候補を見ずに入力する

最初から3形式すべてを同日に強制しない。正答状況、学習時間、表現の難しさに応じて当日または後日の問題へ分配する。

### 慣れたユーザー

最初から文字入力または音声入力を提示する。回答欄で入力方法を切り替えられ、端末や場所の都合で話せない場合は文字入力だけで完了できる。

### 出題形式の決定

- 初回の基準は技能別CEFRと簡易テスト結果から決める
- 表現ごとの実績を優先し、同じユーザーでも問題ごとに形式を変える
- 選択肢で進級条件を満たしたら並べ替えへ進める
- 並べ替えで進級条件を満たしたら文字入力へ進める
- 1回不正解になった時点で一段階戻し、答えを覚えるための再生を挟む
- ユーザーは「かんたんすぎる」から次の形式へ進める
- ユーザーは設定で文字・音声入力を優先できる

選択肢または並べ替えの正解は練習進行には使うが、「ヒントなしの意味確認」には数えない。文字入力または確認済みの音声入力で、訳・候補・文字ヒントを表示せず正解した場合だけ、ヒントなし正解として扱う。

### 進級・降格ルール

| 判定 | 条件 |
|---|---|
| 進級（初挑戦） | 同一形式で、異なる日に2回、追加ヒントなし・初回正解 |
| 降格 | 1回の不正解で即降格し、降格前の上位形式の進級カウントをリセット |
| 再昇格（初回） | 降格先で異なる日に1回、追加ヒントなし・初回正解すれば元の形式へ復帰 |
| 再昇格（2回目以降） | 初回再昇格の恩典は使えない。通常の異なる2日の正解で進級 |
| 「答えを見る」 | 不正解扱いにせず降格しない。進級カウントにも含めない |
| 同日の反復 | 進級カウントは形式ごとに1日1回まで |

問題形式に最初から含まれる選択肢や並べ替え用の単語は、追加ヒントには含めない。「初回正解」は、その出題回の最初の確定回答で正解することを指す。回答を修正して正解した場合は練習完了にはなるが、進級カウントには含めない。

進級カウントは選択肢、単語並べ替え、文字入力の各形式で別々に保持する。音声入力は文字入力と同じ自力回答段階として扱う。

### 初回再昇格恩典の扱い

- 恩典はユーザーと表現の組み合わせごとに1回だけ付与する
- 降格しただけでは消費せず、1回正解で元の形式へ復帰した時点で消費する
- 再昇格後にもう一度降格した場合は、異なる2日の正解を改めて必要とする
- ある表現で恩典を使っても、別の表現の恩典には影響しない
- 形式をユーザーが手動で変更した場合は、降格や恩典消費として扱わない

### 不正解後の再出題

1. 不正解を確定する
2. 正解、文脈上の意味、該当フレーズの音声を確認する
3. 同じ学習回の2〜4問後から、再出題位置をランダムに決める
4. 同じ形式で再出題する
5. 再出題でも不正解なら、もう一度だけ同じ手順で差し戻す
6. 2回の再出題に失敗したら、その表現の当日学習を終了して「要復習」にし、翌日以降は一段易しい形式から始める

再出題で正解しても進級カウントには算入せず、当日のカウント上限は1のままにする。再正解は当日の練習完了として記録できる。当日終了は対象表現だけに適用し、ユーザーは残りの問題を続けられる。

問題列の残りが2問未満の場合は、終了前に再出題できるよう問題を後ろへ追加する。ただし、ユーザーがセッションを終了した場合は無理に延長せず、次回の「要復習」へ送る。

### 対象表現を優先する採点

この問題は一般的な意味伝達テストではなく、動画で出会った対象表現を想起するための問題である。

- `I'm down for that` が対象なら、`I agree` や `Sounds good` は正解にしない
- この場合は「文法的には通じます。動画のセリフ “I'm down for that” で言ってみよう」と表示する
- 短縮形、大小文字、句読点、空白など、対象表現を変えない表記揺れは正解にできる
- 時制、三単現、動詞活用（`-ed`、`-ing`等）の誤りは、意味が推測できても不正解にする
- 言語ごとの活用・語順・助詞が対象表現の学習点なら、省略や置換を正解にしない
- 自由発話練習では自然な言い換えを評価できるが、穴埋めの進級判定とは分ける

### 音声回答

- 発話終了後に即時自動採点し、文字確認や採点ボタンを挟まない
- 判定結果と認識された短い語句を約1.5秒表示する
- 誤認識の場合は「やり直す」を1タップで選べる
- やり直し対象の判定は進級・降格履歴から除外する
- 通常は短い結果表示と音声の後、自動で次へ進む
- 自動で次へ進んだ直後も、直前回答に対する「やり直す」を選べる

### スキップとアーカイブ

スキップ時は次の2つを表示する。

1. あとで再挑戦：当日のセッション末尾へ移動する
2. この表現を対象外にする：アーカイブし、復習キューと自動出題から除外する

アーカイブは確認画面を挟まず即時実行し、数秒間「元に戻す」を表示する。削除ではないため履歴を残し、学習言語ごとの表現一覧にある「対象外」タブから解除できる。ロードマップ必須表現をアーカイブした場合は、同じ学習目標に合う代替表現をAIが提案し、ユーザー承認後に置き換える。

### 中断と再開

- 同じ学習日内なら、問題位置と残りキューから再開する
- 学習日はアカウント設定タイムゾーンの午前4時に切り替える
- 午前0時から3時59分59秒までの学習は前の学習日に含める
- 午前4時を越えたら、進行中の回答を安全に終えてから前回のセッション位置と並び順を破棄する
- 未完了表現は、その日の期限と状態に合わせて今日の復習キューへ統合する
- 完了済み回答、進級、習得、再昇格恩典の履歴は破棄しない

回答後は単なる正誤だけでなく、間違えた部分を短く示す。AI解説は最初から開かず、「解説を見る」をタップした場合だけ表示する。

## 7. 習得判定との関係

ヒントなし穴埋めで文脈上の意味を正しく答えた結果を、意味確認の成功として記録できる。習得済みには次の両方が必要である。

1. 異なる2日でヒントなしの意味確認に正解
2. 発話または文字入力練習を1回完了

同じ日に繰り返した正解は1日分である。習得後に間違えた場合は「要復習」にするが、習得実績と完了済みロードマップは残す。次の復習でヒントなし正解すれば「要復習」を解除し、発話・入力のやり直しは求めない。

## 8. 自動出題

今日の学習を開始すると自動で次の問題へ進む。各問題の音声は設定がオンなら自動再生する。誤操作を防ぐため、回答確定前には自動で次へ進まない。

問題順は次を優先する。

1. 要復習
2. 期限の来た復習
3. 今日の動画で学ぶ主要表現
4. ユーザーが保存した表現

問題数の目安は5分で約5問、10分で約10問、20分で約20問とする。実際の動画再生時間や回答時間を見て調整し、選択時間を超えて残りの全問を強制しない。

最初の不正解では進級状態を仮降格にするが、同じセッション内の再出題は同じ形式で行う。再出題に正解したら仮降格を取り消し、その表現の残りの再出題を中止する。2回の再出題にも失敗した場合だけ降格を確定し、翌日以降は一段易しい形式から始める。

## 9. アクセシビリティ

- 音声なしでも回答可能
- 自動再生は設定で停止可能
- 色だけで正誤を伝えない
- 文字サイズ拡大とスクリーンリーダーに対応
- RTL表示言語でも回答欄と文の方向を言語別に扱う

## 10. 受入条件

1. 動画の実際の一文から穴埋めが生成される。
2. フレーズ音声を何度でも再生できる。
3. ヒント使用後の正解は、ヒントなし正解として記録されない。
4. 初挑戦では同一形式で異なる日に2回、追加ヒントなし・初回正解した場合だけ進級する。
5. 1回の不正解で即降格し、上位形式の進級カウントをリセットする。
6. 最初の降格後だけ、降格先で異なる日に1回正解すれば元の形式へ復帰できる。
7. 再昇格恩典を使った後の再降格では、通常の2日条件へ戻る。
8. 「答えを見る」では降格しない。
9. 同日の進級カウントが形式ごとに1回を超えない。
10. 不正解後、答えと音声の確認を経て2〜4問後に同じ形式で再出題される。
11. 再出題は最大2回で、2回失敗するとその表現は当日終了して「要復習」になる。
12. 再出題の正解が進級カウントに算入されない。
13. 初級者は選択肢、並べ替え、文字入力へ段階的に進める。
14. 慣れたユーザーは文字または音声入力から開始できる。
15. 音声を使えないユーザーが文字入力で完了できる。
16. 解説はユーザーが開いたときだけ表示される。
17. 習得・要復習・解除の状態がLearner Modelと一致する。
18. 言い換えで対象表現を回避した回答を進級の正解にしない。
19. 音声回答が確認操作なしで採点され、1タップでやり直せる。
20. スキップ時に当日再挑戦とアーカイブを選べる。
21. セッション位置は当日中だけ復元され、日付変更後は新しいキューになる。
22. 時制、三単現、動詞活用の誤りが不正解になる。
23. 学習日とストリークが午前4時を境界として計算される。
24. 再出題正解で仮降格が取り消され、2回失敗時だけ翌日の形式が下がる。
