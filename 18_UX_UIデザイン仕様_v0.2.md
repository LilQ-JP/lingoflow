---
document: ux-ui-design-spec
version: 0.2
status: draft-for-review
created: 2026-09-05
updated: 2026-09-05
owner: LilQ
related: 17_パイロットMVP要件定義_v0.2.md
concept_image: Lingoflow_検索・推定理解度・YouTubeプレイヤー_UIコンセプト_v0.2.png
---

# Lingoflow UX/UIデザイン仕様 v0.2

## 1. デザインの狙い

Lingoflowは「勉強画面」より、短い動画に集中しながら次の一歩だけ案内される体験を目指す。親しみやすさは保つが、ゲームの報酬で引っ張らない。

> 動画は没入的、学習操作は明快、復習は一つずつ。

## 2. 参考画像から採用する考え方

### 採用する

- 動画を画面上部の主役にする
- 英語を大きく、日本語訳を一段弱く表示する
- 現在のセリフをカードや背景差で明示する
- 再生操作を片手で届く下部へまとめる
- 練習画面は一問一画面にする
- 学習の進み具合を短い分割バーで示す
- ダーク画面で動画・字幕・発話に集中させる
- 大きな角丸カードと十分な余白を使う

### Lingoflowでは変える

- 隠れたジェスチャーだけに頼らず、「意味」「フレーズ」を表示する
- 動画視聴中に多くの道具を常時並べない
- 学習マップ、宝石、炎、順位などは初期版に入れない
- キャラクターは案内役に留め、回答や動画より目立たせない
- 英文中に日本語を置換せず、英語原文を常に保つ
- 競合の色、アイコン、配置、キャラクターをそのまま再現しない

## 3. ビジュアル方向

### カラー

| 用途 | 色 | 値 |
|---|---|---|
| 視聴背景 | Deep Charcoal | `#0F1210` |
| 練習背景 | Midnight Navy | `#090D18` |
| ホーム背景 | Warm Ivory | `#F7F2E8` |
| ブランド主色 | Muted Forest | `#294A32` |
| 学習中の強調 | Soft Lime | `#A8D978` |
| 発話アクション | Clear Blue | `#356AE6` |
| 主テキスト・暗色面 | Off White | `#F5F5F0` |
| 副テキスト・暗色面 | Mist Gray | `#A9AEA8` |
| エラー | Soft Red | `#E57474` |

緑は「今注目する表現」、青は「声を出す操作」に使い分ける。色だけで状態を示さず、文字、枠、アイコンも併用する。

### 文字

- iOSではシステムフォントを基本とする
- 英語字幕：28〜34pt、Semibold、行間1.25
- 日本語訳：17〜20pt、Regular、行間1.45
- 画面タイトル：22〜28pt、Bold
- ボタン：17pt、Semibold
- 補助ラベル：13〜15pt、Medium
- 動的文字サイズに対応し、字幕は最低2段階まで拡大できる

### 形と間隔

- 基本余白：8ptグリッド
- 画面左右余白：20pt
- カード角丸：20〜28pt
- ボタン高：52〜60pt
- タップ領域：最低44×44pt
- 重要操作同士は最低12pt離す

## 4. 画面別仕様

### S03 今日

目的は「迷わず今日の10分を始める」。

```text
[今日の10分]                         [プロフィール]
毎日、少しずつ話せる自分へ。

┌────────────────────┐
│ おすすめレッスン                         │
│ [動画サムネイル]                         │
│ 初級  4:12                              │
│ カフェで自然に注文する                   │
│ 日本語サポートあり                       │
│ [          はじめる          ]           │
└────────────────────┘

[昨日のフレーズ：I'd like a ..., please.]

[今日]           [学習記録]          [メニュー]
```

- 最上部に検索欄「YouTubeから好きな動画を探す」を置く
- 「旅行」「日常」「友達」「趣味」などのテーマチップを横スクロールで置く
- 大きな主カード1件と、横スクロールの候補カードを最大5件表示する
- 解析済みカードには推定理解度を円形ゲージと数字で表示する
- 数字には必ず「推定」を付け、タップすると根拠を表示する
- 前日の復習が未完了でも、新しい動画を完全にはロックしない

### S13 動画検索・おすすめ

```text
[戻る]  [ YouTubeから動画を検索        ]

[おすすめ] [旅行] [日常] [友達] [趣味]

あなたに合いそう
┌────────────────────┐
│ [YouTubeサムネイル]                       │
│ NYC Coffee Shop Vlog                     │
│ 推定理解度 84%  日本語サポートあり  4:12  │
│ 新しい表現 3個                            │
└────────────────────┘

好きな動画を探す
[検索結果カード ...]
```

- 理解度はカードの判断材料の一つとし、最大の文字にはしない
- 80%以上は「そのまま挑戦」、60〜79%は「ちょうどいい」、59%以下は「サポート多め」と補助ラベルを付ける
- 色だけで良し悪しを示さず、難しい動画も選択可能にする
- 未解析カードは「推定理解度：解析後に表示」とする
- 検索結果にはYouTube由来の情報とLingoflowの推定情報を視覚的に分けて表示する

### S04 動画視聴

目的は「動画を理解し、今の表現に気づく」。

```text
[戻る]       カフェで自然に注文する       […]

┌────────────────────┐
│                  VIDEO                   │
└────────────────────┘

Can I get this to go?
これを持ち帰りにできますか？

          [ to go ]
           持ち帰りに

       [↻ フレーズ]    [▢ 意味]

┌────────────────────┐
│ 1:36  ━━━━━━━  4:12                      │
│   10秒戻る    再生/停止    10秒進む   1.0x │
└────────────────────┘
```

- 縦持ちを基本とし、動画は16:9を保つ
- 動画領域はYouTube公式プレイヤーとして扱い、その上にLingoflow独自の字幕や操作を重ねない
- YouTube側の再生操作と競合しないよう、Lingoflowの字幕・フレーズ操作はプレイヤー下へ置く
- 字幕は動画へ重ねず、動画下の専用領域を標準とする
- 現在セリフが変わっても、ユーザーが意味パネルを開いている間は勝手に閉じない
- 「意味」は短いボトムシート。詳細文法は二段目に置く
- スクラブ中は字幕を即時追従させ、離した時点で再生位置を確定する
- 字幕データのない区間では「この区間の字幕はありません」と表示する
- 「フレーズ」は1回タップで対象フレーズの開始直前へ戻す
- 長押しまたは横のメニューで「1回／リピート」を切り替える
- リピート中は対象範囲を淡い枠で示し、停止または通常シークで解除する

### 字幕サポートの3状態

| 状態 | 英語 | 日本語 | 操作 |
|---|---|---|---|
| Full | 全文 | 全文訳 | 対象表現を強調 |
| Hint | 全文 | 対象表現の短い意味のみ | 全訳を一時表示可能 |
| English | 全文 | 非表示 | タップで訳を一時表示 |

サポートが切り替わったときは「今日はヒントだけで見てみましょう」と短く知らせ、設定から戻せるようにする。

### S05 表現解説

- 表現、読み上げ、短い意味、動画中の文、使う場面を一画面に収める
- 最初に見せる説明は2〜3行以内
- 「似た表現」「詳しい文法」は折りたたむ
- 主操作は「自分で使う」

### S06 穴埋め

- 1問ずつ表示する
- 選択肢は最大4つ
- 正解時は色だけでなく、正解文を読み上げる
- 不正解時は責める表現を使わず、元のセリフへ戻れるようにする

### S07 発話／入力

```text
[閉じる]       使って覚える        2 / 3
[進捗バー]

声に出して練習してみよう

┌────────────────────┐
│ Can I get this to go?                   │
│ これを持ち帰りにできますか？             │
└────────────────────┘

[もう一度]         [マイク]         [テキスト入力]

[              言ってみる              ]
```

- マイクを押す前に録音状態を明確にする
- 認識中、成功、聞き取れない、権限なしを別状態として設計する
- 「聞き取れませんでした」は発音の誤りと断定しない
- 文字入力は補助機能ではなく、同等に完了できる代替手段とする

### S08 翌日復習

- 最初に元動画の短い場面を一度だけ再生する
- 1回目は英語字幕の対象表現を伏せる
- 「ヒントを見る」で日本語の短い意味を表示する
- 回答後に全文、訳、前回自分が作った文を表示する
- 「まだ難しい」「もう少しでできそう」「できた」をユーザーが修正できる

### S09 完了

- 完了を数字や報酬より、できるようになった表現で伝える
- 例：「今日は `to go` を使って注文できました」
- 次の予定を一つだけ示す
- SNS共有は置かない

### S11 動画を追加

- 今日画面の「YouTube動画を追加」またはiOS共有シートから開く
- URL入力欄、貼り付け、追加ボタンだけを主画面に置く
- 追加前に「動画によって使える学習機能が異なります」と短く示す
- URLが無効、動画が見つからない、すでに追加済みの場合を入力欄の近くに表示する

### S12 解析状態

```text
[動画サムネイル]
Study Vlog ...

[✓] 動画を確認
[✓] 字幕を確認
[…] 表現を準備中
[ ] 学習画面を作成

使えるようになったら知らせる  [ON]
```

- 技術名や内部ログではなく、ユーザーが待つ理由を示す
- 「学習可能」「字幕準備中」「視聴のみ」「利用不可」を色だけでなくラベルで示す
- 処理失敗時は再試行、字幕提供、別動画の3択を状況に応じて提示する
- 視聴のみの場合、将来必ず学習可能になると約束しない

## 5. ナビゲーション

下部タブは「今日」「学習記録」「メニュー」の3つとする。動画・問題・発話中はタブを隠し、閉じる操作と進捗を表示する。

戻る操作で回答内容や再生位置が失われる場合は確認する。通常の画面遷移では確認ダイアログを出さない。

## 6. 状態・例外

| 状態 | 表示と操作 |
|---|---|
| 読み込み中 | 骨組みを保ったスケルトン表示。操作を跳ねさせない |
| オフライン | 保存済み範囲を示し、再試行を表示 |
| 動画再生不可 | 理由を断定せず、別教材へ移動できる |
| 字幕なし区間 | 専用メッセージを表示し、動画は継続 |
| 音声認識失敗 | 再試行、自己申告、文字入力を並列提示 |
| マイク権限なし | 設定への導線と文字入力を表示 |
| 復習なし | 次回予定と、新しい動画への導線を表示 |
| 長文字幕 | 3行を超える場合はサイズを下げず、スクロールまたは区切りを使う |

## 7. アクセシビリティ

- 文字と背景のコントラストはWCAG AA相当を目標にする
- VoiceOverの読み上げ順を、タイトル→英語→日本語→操作にする
- アイコンのみのボタンへアクセシビリティラベルを付ける
- 色覚に依存せず、対象表現は枠・太字・ラベルでも示す
- 自動再生、音声、触覚フィードバックを設定で止められる
- 字幕表示領域がDynamic Typeで欠けないことを確認する

## 8. デザイン成果物として次に必要なもの

1. 上記13画面の低忠実度ワイヤーフレーム
2. S03・S04・S07・S08の高忠実度デザイン
3. Full／Hint／Englishの字幕状態とフレーズリピート状態の比較
4. 音声認識4状態のコンポーネント
5. 色・文字・余白・ボタン・カード・アイコンの小規模UIキット
6. iPhone SE相当とiPhone 16 Pro相当での表示確認
7. 主要フローを接続したタップ可能プロトタイプ

## 9. 添付参考画像の管理

今回の3枚は、次の点を共有するための参考資料とする。

- 参考1：明るいホーム、一問一画面、カード型プレイヤーの方向
- 参考2：動画＋大きな二言語字幕＋下部操作の没入感
- 参考3：大きな練習カード、短い進捗、発話中心の集中画面

競合の画面を成果物へ転載する場合は、社内参考であること、出典、取得日を記録する。公開資料やストア素材にはそのまま使用しない。
