---
document: youtube-search-channel-follow-spec
version: 0.1
status: draft-for-technical-review
created: 2026-09-06
updated: 2026-09-06
owner: LilQ
related:
  - 17_パイロットMVP要件定義_v0.2.md
  - 18_UX_UIデザイン仕様_v0.3.md
  - 20_YouTube動的追加・字幕処理設計_v0.1.md
  - 21_動画推薦・推定理解度仕様_v0.1.md
---

# Lingoflow YouTube検索・チャンネルフォロー仕様 v0.1

## 1. 目的

ユーザーが、AIのおすすめだけでなく、自分の好きなテーマ・動画・YouTubeチャンネルから学習素材を見つけられるようにする。チャンネルをフォローすると、そのチャンネルの新着動画をLingoflow内で発見できる。

## 2. 用語

- YouTube検索：YouTube上の公開動画・チャンネル候補を探す機能
- Lingoflowフォロー：Lingoflow内でチャンネルを追跡する機能
- YouTubeチャンネル登録：YouTubeアカウント上の登録。初期版では操作しない
- 学習可能：字幕供給、加工、AI処理など必要条件を満たした状態

## 3. 検索対象

検索画面で「動画」「チャンネル」を切り替えられる。

### 動画検索結果

- サムネイル
- タイトル
- チャンネル名
- 動画時間
- 公開時期
- 推定理解度または解析状態
- 学習可能／字幕準備中／視聴のみ
- 新しい表現数

### チャンネル検索結果

- チャンネル画像
- チャンネル名
- 説明の短縮表示
- Lingoflow内で学習可能な動画数
- フォロー操作

## 4. 検索フロー

```text
検索語またはテーマ
  → YouTube検索結果を取得
  → ブロック・重複・再生可否を確認
  → 既存解析結果を照合
  → 解析済みは推定理解度を表示
  → 未解析は解析開始または視聴のみ
  → 選択した動画を学習ライブラリへ追加
```

検索語候補には、選択した目標やテーマを使用する。生の検索語を学習モデルへ使う場合は、保存範囲と削除方法を明示する。

## 5. 検索結果の並び順

「おすすめ順」「新しい順」「短い順」を用意する。

おすすめ順は、YouTube検索結果をそのまま複製せず、次を使ってLingoflow内で並べ替える。

- テーマ一致
- 推定理解度
- 字幕・学習機能の利用可能性
- 動画時間
- 過去に好んだチャンネル
- 新しさ

検索結果がYouTube由来であることを示し、Lingoflowの推定情報と分ける。

## 6. チャンネルフォロー

### フォローする場所

- チャンネル検索結果
- 動画カードのチャンネル名
- 動画視聴画面の詳細メニュー
- チャンネル詳細画面

### フォロー後

- 検索画面上部へチャンネル画像を横並びで表示する
- フォロー中タブに新着動画を日付順で表示する
- 学習可能になった動画を優先表示する
- チャンネルごとに通知をオン／オフできる
- フォロー解除後も、すでに保存した学習記録は残す

### 自動処理の制限

フォローした全チャンネルの全動画を自動でAI解析すると、APIクォータとAI費用が増える。初期は次の順で解析する。

1. ユーザーが「この動画で学ぶ」を押した動画
2. ユーザーのレベル・テーマに強く合う新着候補
3. 運営が全体向けに選んだ動画

残りはメタデータだけ保持し、「解析すると推定できます」と表示する。

## 7. 通知

通知の初期値はオフとし、フォロー操作と同時に自動許可要求を出さない。

通知候補：

- フォロー中チャンネルから、学習可能でレベルに合う動画が追加された
- ユーザーが解析を依頼した動画の準備が完了した

単なる新着公開だけでは通知せず、Lingoflowで学習できる状態になったときに送る。

## 8. データ

```text
FollowedChannel
- user_id
- youtube_channel_id
- display_name_snapshot
- followed_at
- notification_enabled
- last_seen_published_at

ChannelVideoCandidate
- youtube_channel_id
- video_id
- published_at
- metadata_checked_at
- processing_state
- recommendation_score
- selected_for_processing_reason
```

YouTubeの表示データは利用条件に従って更新・削除する。長期間固定保存できるとは前提にしない。

## 9. API・負荷対策

- 検索入力は確定操作または短い待ち時間後に送信し、1文字ごとにAPIを呼ばない
- 同じ検索とページの短時間キャッシュを使う
- 新着確認は全チャンネル同時ではなく分散実行する
- フォローチャンネル数にパイロット上限を設ける
- APIクォータ切れでも保存済み学習と視聴履歴を使えるようにする
- クォータ超過を一般的な通信エラーとして隠さず、運営監視へ記録する

## 10. エラー

| 状態 | 表示 | 操作 |
|---|---|---|
| 検索結果なし | 別の言葉やテーマを提案 | 再検索 |
| API一時停止 | 検索を一時利用できません | 再試行／保存済みを見る |
| 動画非公開 | この動画は現在利用できません | 別動画 |
| チャンネル削除 | フォロー情報を更新できません | フォロー解除 |
| 解析対象外 | この動画は視聴のみ利用できます | 視聴／別動画 |

## 11. 受け入れ条件

1. 日本語・英語・韓国語を含む検索語で動画候補を表示できる。
2. 動画とチャンネルの結果を区別できる。
3. チャンネルをフォローし、フォロー中一覧へ反映できる。
4. 新着動画を公開日時順に表示できる。
5. 解析済み動画だけに推定理解度を表示する。
6. 動画選択から追加・解析・視聴へ正しく遷移する。
7. フォロー解除後も既存の学習記録が消えない。
8. APIクォータや通信失敗時に、再試行以外の行動を選べる。

## 12. 未決事項

- パイロット中のフォローチャンネル上限
- YouTubeアカウント連携と登録チャンネルの取り込みを将来行うか
- Shorts、ライブ配信、プレミア公開の表示
- 不適切動画・子ども向け動画・地域制限のフィルター
- チャンネル新着確認の周期
- YouTube APIの保持・更新条件に合わせたメタデータ運用

